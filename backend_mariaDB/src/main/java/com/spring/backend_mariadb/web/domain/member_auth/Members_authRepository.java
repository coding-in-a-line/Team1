package com.spring.backend_mariadb.web.domain.member_auth;

import org.springframework.data.jpa.repository.JpaRepository;

public interface Members_authRepository extends JpaRepository<Members_auth, Long> {
}
